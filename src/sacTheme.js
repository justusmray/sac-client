import { createMuiTheme } from "@material-ui/core";

const sacTheme = createMuiTheme({
    palette: {
        primary: {
            main: '#053856'
        },
        secondary: {
            main: '#ff6600'
        }
    }
})

export default sacTheme;